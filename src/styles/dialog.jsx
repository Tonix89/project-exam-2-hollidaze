import theme from './theme';

export const dialogStyle = {
  border: '2px solid',
  borderColor: theme.palette.secondary.main,
  borderRadius: '10px',
  boxShadow: 24,
  width: { xs: '90%', md: '30%' },
};
