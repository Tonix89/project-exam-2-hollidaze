import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#002804",
    },
    secondary: {
      main: "#C08631",
    },
    light: {
      main: "#E4E4EB",
    },
    grey: {
      main: "#BDBDBD",
    },
  },
});

export default theme;
