import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main : "#002804",
        },
        secondary: {
            main: "#C08631",
        }
    }
});

export default theme;